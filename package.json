{
  "name": "peerjs-server",
  "version": "1.0.0",
  "main": "index.js",
 "scripts": {
  "start": "cross-env PORT=$PORT peerjs --port $PORT --key peerjs --path /myapp"
},
  "dependencies": {
    "peer": "^0.6.1"
  }
}
